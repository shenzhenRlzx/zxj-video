{
  "name": "zxj-video-v4",
  "version": "0.1.0",
  "description": "Electron + sql.js barcode scanner",
  "main": "main.js",
  "author": "",
  "license": "MIT",
  "scripts": {
    "start": "electron .",
    "clean:win": "node -e \"require('fs').rmSync('dist\\\\win-unpacked', { recursive: true, force: true });\"",
    "build:win": "npm run clean:win && set CSC_IDENTITY_AUTO_DISCOVERY=false&& set ELECTRON_BUILDER_CACHE=%CD%\\dist\\.electron-builder-cache&& electron-builder --win --x64"
  },
  "dependencies": {
    "serialport": "^13.0.0",
    "sql.js": "^1.9.0"
  },
  "devDependencies": {
    "electron": "^30.0.1",
    "electron-builder": "^24.13.3"
  },
  "build": {
    "appId": "com.zxj.barcode.scanner",
    "productName": "ZXJ Barcode Scanner",
    "files": [
      "main.js",
      "hikvision.js",
      "preload.js",
      "toast-preload.js",
      "database.js",
      "utils.js",
      "renderer/**/*",
      "node_modules/**/*"
    ],
    "asar": true,
    "win": {
      "target": [
        "nsis",
        "portable"
      ]
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    }
  }
}
